*Internet Control Message Protocol (ICMP)* es un protocolo utilizado para comunicar información a nivel de red. Permite reportar errores, por ejemplo, `host unreachable`, `network unreachable`, `port unreachable` o `protocol unreachable`. O mensajes como el `echo request` y el `echo reply` (utilizados por el comando $\texttt{ping}$).

